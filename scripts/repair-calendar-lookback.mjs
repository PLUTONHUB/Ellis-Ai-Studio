import {readFile,writeFile} from 'node:fs/promises';
const e={};for(const f of ['.env','.env.local','.data/n8n.env'])try{for(const l of (await readFile(f,'utf8')).split(/\r?\n/)){const i=l.indexOf('=');const k=i>0?l.slice(0,i).trim():'';if(k&&!(k in e))e[k]=l.slice(i+1).trim();}}catch{}
const base=e.N8N_BASE_URL.replace(/\/$/,'');const h={'X-N8N-API-KEY':e.N8N_API_KEY,'Content-Type':'application/json'};async function api(m,p,b){const r=await fetch(base+'/api/v1'+p,{method:m,headers:h,body:b?JSON.stringify(b):undefined}),t=await r.text();if(!r.ok)throw Error(t);return t?JSON.parse(t):{}}
const w=await api('GET','/workflows/7SdaEBilKG5ufROO');await writeFile('artifacts/phase1-before-calendar-lookback.json',JSON.stringify(w,null,2));const bk=await api('POST','/workflows',{name:'Ellis Phase 1 PRIVATE TEST backup before calendar lookback repair',nodes:w.nodes,connections:w.connections,settings:w.settings});const n=w.nodes.find(x=>x.name==='Get Scheduled Calendar Events');n.parameters.timeMin='={{ $now.minus({ minutes: 10 }) }}';n.parameters.timeMax='={{ $now }}';n.parameters.options={};await api('PUT','/workflows/'+w.id,{name:w.name,nodes:w.nodes,connections:w.connections,settings:w.settings});console.log(JSON.stringify({backupId:bk.id,node:n.id}));
