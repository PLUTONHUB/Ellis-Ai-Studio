import { createFileRoute } from "@tanstack/react-router";
import { StandardPage, services } from "~/components/growth-site";
import { pageHead } from "~/lib/seo";
export const Route = createFileRoute("/services")({ head: () => pageHead({ title: "AI Automation, Websites & CRM Systems for Service Businesses | Ellis", description: "Explore AI websites, lead recovery, CRM automation, custom software, integrations, and business intelligence for service businesses.", path: "/services" }), component: () => <StandardPage eyebrow="Services" title="AI systems that turn service-business growth goals into dependable workflows." intro="Choose the commercial problem you need to solve: create more qualified demand, protect every lead, or eliminate the manual work slowing your team down."><div className="service-grid">{services.map(([t,d],i)=><article className="service-card" key={t}><span>0{i+1}</span><h2>{t}</h2><p>{d}</p><a href={`/services/${t.toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/-$/, "")}`}>Explore {t}</a></article>)}</div></StandardPage> });
