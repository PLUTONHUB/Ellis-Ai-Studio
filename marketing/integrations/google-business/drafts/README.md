# Draft Records

Store integration-created draft manifests here only when a future runtime exists; final marketing copy belongs in `marketing/content-library/drafts/`. Every record must link its source event, location snapshot, shared inputs, canonical Google Business template, reviewer, and approval state.

Draft generation is permitted only after source verification. Publishing is not permitted from this directory.
